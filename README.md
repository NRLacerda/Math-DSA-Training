# ∑ sharp-dsa

> *A personal C# scratchpad for math foundations and data structures & algorithms.*

```
Language  →  C# 
Purpose   →  Practice, experimentation
```

---

## What's this?

Me grinding through math and DSA concepts in C#. 

Basically an digital notebook

---

## Structure

We don't actually have this structure, but it's a goal, lol

```
sharp-dsa/
├── Math/
│   ├── Primes/          # Sieve of Eratosthenes, primality tests
│   ├── Modular/         # Modular arithmetic, fast exponentiation
│   ├── Combinatorics/   # Permutations, combinations, Pascal's triangle
│   └── Matrices/        # Gaussian elimination, transformations
│
├── DataStructures/
│   ├── LinkedList/      # Singly, doubly, circular
│   ├── Stack/           # Array-backed and linked
│   ├── Queue/           # Standard + circular buffer
│   ├── BinaryTree/      # BST, traversals, balancing
│   ├── Heap/            # Min/max heap, heapify
│   ├── HashTable/       # Collision handling strategies
│   └── Graph/           # Adjacency list/matrix representations
│
├── Algorithms/
│   ├── Sorting/         # Merge, quick, heap, radix
│   ├── Searching/       # Binary search, interpolation
│   ├── Graph/           # BFS, DFS, Dijkstra, A*
│   ├── DynamicProg/     # Memoization and tabulation patterns
│   └── Backtracking/    # N-Queens, sudoku solver
│
└── Challenges/          # Random problems I felt like solving
```

---

## Philosophy

- **Reinvent the wheel** — the point is to understand it, not ship it
- **Comment the why**, not the what
- **Measure complexity** — every non-trivial implementation gets its Big O noted
- **Keep it readable** — future me will thank present me

---

## Notes to self

```
// If it compiles it ships (just kidding, please test)
// O(n²) is acceptable while learning, not forever
// When in doubt, draw the recursion tree
```

---

*Nicolas — ongoing*